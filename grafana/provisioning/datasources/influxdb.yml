apiVersion: 1

datasources:
  - name: InfluxDB
    uid: InfluxDB
    type: influxdb
    access: proxy
    url: http://sidekick-influxdb:8086
    jsonData:
      version: Flux
      organization: sidekick
      defaultBucket: sidekick
      tlsSkipVerify: true
    secureJsonData:
      token: ${SIDEKICK_INFLUX_TOKEN}
    isDefault: true
    editable: true
