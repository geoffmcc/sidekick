# Tree-sitter grammars publish optional peer ranges for different runtime
# generations. Sidekick loads all pinned grammars through the single pinned
# tree-sitter runtime and verifies them in the semantic parser test suite.
legacy-peer-deps=true
