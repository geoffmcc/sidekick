# Reproducibility Bundles

Reproducibility bundles contain bounded repository profile and semantic-index verification output. They are serialized through the Security Research evidence custody path, marked redacted, and never include raw source, environment files, credentials, cookies, or unbounded command output.

The bundle proves what was observed at capture time. It does not recreate a build or claim that external services are equivalent.
