# Infrastructure as Code

This pack is deliberately plan-only. It composes configured Compose inspection and the structured parser, preserves provider validation as untrusted evidence, and does not expose apply, update, lifecycle, network or destroy operations. A plan is not an authorization to mutate infrastructure.
