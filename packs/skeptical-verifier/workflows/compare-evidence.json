{
  "schema_version": 1, "name": "skeptical-verifier/compare-evidence", "version": "1.0.0", "title": "Compare evidence independently", "description": "Compare two supplied observations and preserve disagreement for interpretation.", "mode": "read_only", "inputs": { "baseline": { "type": "object", "required": true }, "candidate": { "type": "object", "required": true } }, "steps": [{ "name": "compare", "tool": "skeptical_compare", "args": { "baseline": "${inputs.baseline}", "candidate": "${inputs.candidate}", "mode": "json" }, "expect": "json", "on_error": "fail" }], "result": { "comparison": "${steps.compare.json}" }, "tags": ["verification", "evidence", "read-only"]
}
