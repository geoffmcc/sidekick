# API Engineering and Contract Testing

Every API target is caller-supplied but must be bound to an operator-created named network scope. Assertions are bounded and deterministic; page or response content is untrusted evidence. The pack performs no API writes, credential changes or deployment actions. Capture evidence only when the caller explicitly requests it.
