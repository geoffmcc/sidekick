{
  "schema_version": 1, "name": "testing-quality-engineering/quality-lifecycle", "version": "1.0.0", "title": "Quality lifecycle", "description": "Plan or execute governed verification and report unsupported cancellation honestly.", "mode": "read_only", "inputs": { "action": { "type": "string", "required": true }, "path": { "type": "string", "required": true }, "mode": { "type": "string", "required": false } }, "steps": [{ "name": "lifecycle", "tool": "quality_lifecycle", "args": { "action": "${inputs.action}", "path": "${inputs.path}", "mode": "${inputs.mode}" }, "expect": "json", "on_error": "fail" }], "result": { "lifecycle": "${steps.lifecycle.json}" }, "tags": ["testing", "quality", "lifecycle"]
}
