# Operational Readiness

Readiness is fail-closed. `operational_readiness` refuses to assess without attributable evidence references, inspects those references through research evidence custody, and then reads current platform health, repository profile, and formal continuity state.

The pack never turns a successful tool call into a `ready` claim. Operators must apply service-specific acceptance criteria, freshness, rollback, dependency, and endpoint checks.
