# Pinned dependencies for Sidekick OpenVINO NPU Helper
# Install in a clean Python 3.12.13 environment.
#
# transformers 5.5.0 or later is required to address the unsafe LightGlue
# model-loading path (security advisory). The prior 4.57.6 correctness
# baseline should be re-run after installing this upgraded runtime.
openvino==2026.2.1
transformers==5.10.1
numpy==1.26.4
