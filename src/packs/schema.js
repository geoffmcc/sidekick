"use strict";

// Compatibility location retained for package lifecycle callers. Ownership is
// platform-level; the implementation lives beside the platform kernel.
module.exports = require("../platform/capability-pack-schema");
