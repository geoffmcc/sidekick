"use strict";

// The lifecycle executor is the single live Agent certification boundary.
// Keep this module as a narrow compatibility import for existing consumers.
module.exports = require("./lifecycle");
